## What

## Why

## Checklist

- [ ] `pnpm lint` / `pnpm typecheck` / `pnpm test` pass
- [ ] `pnpm format:check` passes (or ran `pnpm format`)
- [ ] If menu behavior or order changes: updated `src/root-menu.ts` and considered Graphite docs in `docs/graphite-resources.md`
